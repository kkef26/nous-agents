{
  "name": "conductor-merge-gates",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NOUS.CONDUCTOR.MERGE_GATES.1 — live staging-HEAD merge invariant. Fetches the origin/staging tip from the GitHub REST API immediately before every PATCH merge request; fetch failure = hard abort; 409 = merge_conflict logged, clause not advanced.",
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "node --test --import tsx ../../tests/NOUS.CONDUCTOR.MERGE_GATES.1.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
